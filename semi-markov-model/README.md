# Semi-Markov Models in HEOR Lab

The application is a self-contained static web app. Its complete HTML, CSS and JavaScript are in `index.html`.

Published path: `/semi-markov-model/`

The lab contains 10 tutorial modules plus an integrated worked exercise.
